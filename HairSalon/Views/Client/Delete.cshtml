@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2>Lez Delete, no?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)

@using(Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn"/>
}
@Html.ActionLink("Back to the Salon", "Index")